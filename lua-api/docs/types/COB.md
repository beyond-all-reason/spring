---
layout: default
title: COB
parent: Lua API
permalink: lua-api/types/COB
---

# enum COB
---



[<a href="https://github.com/beyond-all-reason/spring/blob/95d591b7c91f26313b58187692bd4485b39cb050/rts/Lua/LuaConstCOB.cpp#L22-L101" target="_blank">source</a>]


```cpp
enum COB {
    ABS = number, 
    ACTIVATION = number, 
    AIR_LOS_RADIUS = number, 
    ALPHA_THRESHOLD = number, 
    ARMORED = number, 
    ATAN = number, 
    BUGGER_OFF = number, 
    BUILD_PERCENT_LEFT = number, 
    BUSY = number, 
    CEG_DAMAGE = number, 
    CHANGE_TARGET = number, 
    CLOAKED = number, 
    COB_ID = number, 
    CRASHING = number, 
    CURRENT_FUEL = number, 
    CURRENT_SPEED = number, 
    DO_SEISMIC_PING = number, 
    FLANK_B_DIR = number, 
    FLANK_B_MAX_DAMAGE = number, 
    FLANK_B_MIN_DAMAGE = number, 
    FLANK_B_MOBILITY_ADD = number, 
    FLANK_B_MODE = number, 
    GAME_FRAME = number, 
    GROUND_HEIGHT = number, 
    GROUND_WATER_HEIGHT = number, 
    HEADING = number, 
    HEALTH = number, 
    HYPOT = number, 
    INBUILDSTANCE = number, 
    IN_WATER = number, 
    JAMMER_RADIUS = number, 
    KILL_UNIT = number, 
    LAST_ATTACKER_ID = number, 
    LOS_RADIUS = number, 
    MAX = number, 
    MAX_ID = number, 
    MAX_SPEED = number, 
    MIN = number, 
    MY_ID = number, 
    ON_ROAD = number, 
    PIECE_XZ = number, 
    PIECE_Y = number, 
    PLAY_SOUND = number, 
    POW = number, 
    PRINT = number, 
    RADAR_RADIUS = number, 
    REVERSING = number, 
    SEISMIC_RADIUS = number, 
    SET_WEAPON_GROUND_TARGET = number, 
    SET_WEAPON_UNIT_TARGET = number, 
    SHIELD_POWER = number, 
    SONAR_JAM_RADIUS = number, 
    SONAR_RADIUS = number, 
    SONAR_STEALTH = number, 
    STANDINGFIREORDERS = number, 
    STANDINGMOVEORDERS = number, 
    STEALTH = number, 
    TARGET_ID = number, 
    TRANSPORT_ID = number, 
    UNIT_ALLIED = number, 
    UNIT_BUILD_PERCENT_LEFT = number, 
    UNIT_HEIGHT = number, 
    UNIT_TEAM = number, 
    UNIT_XZ = number, 
    UNIT_Y = number, 
    UPRIGHT = number, 
    VETERAN_LEVEL = number, 
    WANT_CLOAK = number, 
    WEAPON_ACCURACY = number, 
    WEAPON_PROJECTILE_SPEED = number, 
    WEAPON_RANGE = number, 
    WEAPON_RELOADSTATE = number, 
    WEAPON_RELOADTIME = number, 
    WEAPON_SPRAY = number, 
    XZ_ATAN = number, 
    XZ_HYPOT = number, 
    YARD_OPEN = number, 
    
}
```
