### Caveats
 * renamed `springCategories` to `summerCategories`. Run `scripts/seasons-greetings.sh` provided in `(basecontent)/dev/null` on your repo to automatically update your game

### Fixes
 * fixed the passage of time not being obeyed correctly