 * add `GroundShadingRateElmos` float config (default 0 = off): ground footprint in elmos per pixel beyond which distant terrain is shaded at coarser rates (once per 2x2 pixels past twice, once per 4x4 past four times the value). Requires `GL_NV_shading_rate_image` (NVIDIA Turing or newer), a no-op elsewhere.
 * add `Spring.SetGroundShadingRateElmos(number elmosPerPixel) → boolean supported` for runtime control of the same threshold (0 disables).
 * add `Platform.glSupportShadingRateImage` boolean.
